/**
 * Creates a performance mark with the given name for timing measurements.
 */
export type Mark = Performance["mark"];

/**
 * Measures the duration between two performance marks or from a mark to now.
 */
export type Measure = Performance["measure"];
