import type { CreateManagedReactiveScroller } from "./factories/createManagedReactiveScroller";

/**
 * A `ManagedReactiveScroller` that automatically pins to the bottom.
 */
export type PinToBottomScrollerAuto = ReturnType<CreateManagedReactiveScroller>;
