/**
 * The start time of the navigation in milliseconds since the epoch.
 */
export type NavigationStart = number;

/**
 * The time origin in milliseconds since the epoch.
 */
export type TimeOrigin = number;

/**
 * The global performance object, which provides access to performance-related information.
 */
export type Performance = globalThis.Performance;
