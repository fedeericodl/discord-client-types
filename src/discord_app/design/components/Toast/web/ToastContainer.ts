import type * as React from "react";

import type { UseToastStore } from "./ToastAPI";

/**
 * The container used to display toasts. Used to work with the {@link UseToastStore toast store}.
 */
export type ToastContainer = React.FC;
